// examples/demo.ltl — runnable demo for ltl_attention_toy

import std::io
use ltl_attention_toy as lib

fn main() {
    io::println("ltl_attention_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}