// ltl-attention-toy/main.ltl
//
// Scaled dot-product attention over toy embeddings.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-attention-toy v0.1.0")
    io::println("Scaled dot-product attention over toy embeddings.")
    0
}
